go mod tidy

----
1、关于图片识别的路径问题；
2、关于chromium的编译问题
3、关于 kill的进程问题。
4、关于在线的chromium的编译问题。

------


bug:
1、我发现貌似如果图片在那么他截图不会成功。我不知道是不是有文件占用的情况。
    因为是偶尔出现、所以我先记录下来，后面再说。


1、支持视觉方式-
    操作浏览器进行访问、点击、拖拽、文本输入、
2、抗指纹:
    修改user-agent、语言、时区
    修改cavans指纹、
        压缩级别
        色彩深度
    修改webgl指纹
    修改声卡指纹、
    修改插件指纹、
    修改字体指纹
    防止IP探查、
    webdriver-防判断；
    headless-防判断
    最小化窗口-隐藏窗口-防止判断
    鼠标移动、轨迹模拟人类方式、
       鼠标键盘、trusted=true;
    键盘模拟了、输入法方式输入
    修复隐身模式下window.navigator.webdriver=true问题;


3、支持多种方式设置代理IP
4、支持多进程并发
5、支持多用户用户数据隔离
6、支持cookie保持、
7、支持抓包、修改http、https协议请求;
8、支持tls指纹、访问；
9、支持Chrome dev tools 协议自定义;
10、支持拦截请求、修改请求内容;
    -支持一一对应的request-response请求;
    -支持协议层彻底拦截请求-处理请求;
-
适合做什么？
1、爬虫-尤其是反爬虫难道比较高的爬虫;
2、批量账号注册
3、抢购程序
4、刷票、模拟点击、刷赞、偷菜;
5、游戏外挂;
--只要你基于浏览器、几乎无所不能;